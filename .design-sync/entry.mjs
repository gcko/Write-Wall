// design-sync bundle entry: Write-Wall is a tokens-only design system (no
// React components) — this empty module satisfies the converter's entry
// requirement so it resolves the package root and ships styles.css only.
export {};
